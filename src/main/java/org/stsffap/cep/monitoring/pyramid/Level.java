package org.stsffap.cep.monitoring.pyramid;

import java.util.ArrayList;

import org.apache.flink.api.java.tuple.Tuple2;
import org.apache.flink.streaming.api.datastream.DataStream;
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;

public  class Level {
    /* private ArrayList<Tuple2<Event, Transformation>> level;
    
    
    public  void add(Event event, Transformation transformation, StreamExecutionEnvironment env){
        this.level.add(new Tuple2<Event, Transformation>(event, transformation));
        DataStream<Event> dataStream = transformation();
    } */
}